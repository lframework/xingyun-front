export const mm2px = (mm) => {
  return (mm * 96) / 25.4;
};

export const px2mm = (px) => {
  return (px * 25.4) / 96;
};

export const px2pt = (px) => {
  return (px * 72) / 96;
};

export const pt2px = (pt) => {
  return (pt * 96) / 72;
};
